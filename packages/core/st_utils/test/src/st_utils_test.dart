// ignore_for_file: prefer_const_constructors
import 'package:st_utils/st_utils.dart';
import 'package:test/test.dart';

void main() {
  group('StUtils', () {
    test('can be instantiated', () {
      expect(StUtils(), isNotNull);
    });
  });
}
